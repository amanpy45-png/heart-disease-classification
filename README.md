# heart-disease-classification
This project implements a neural network from scratch using NumPy to predict heart disease based on clinical patient data. It includes data preprocessing, feature scaling, one-hot encoding, forward propagation, backpropagation, and model evaluation. The goal is to demonstrate how neural networks work without using deep learning frameworks.
